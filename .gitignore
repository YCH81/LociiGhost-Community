# Node / Astro (W3)
node_modules/
dist/
.astro/
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Build / coverage
build/
coverage/

# Supabase local
supabase/.branches/
supabase/.temp/

# Per-developer Claude Code launch config (local-only, points at this
# absolute path on this machine). Each contributor can add their own
# under ~/.claude or their workspace root.
.claude/
